package com.github.jameshnsears.chance.data.sample.settings

import com.github.jameshnsears.chance.data.domain.state.Settings

class SampleSettings {
    companion object {
        val settings = Settings(
            tabRowChance = 0,
        )
    }
}
