package com.github.jameshnsears.chance.ui.tab.roll

import com.github.jameshnsears.chance.utility.android.UtilityAndroid
import kotlinx.coroutines.test.runTest
import org.junit.Assert.fail
import org.junit.Test


class TabRollViewModelUnitTest : UtilityAndroid() {
    @Test
    fun detailRollDiceCards() = runTest {
        // check Dice produce correct icons and tick selection
        fail("todo")
    }

    @Test
    fun rollDiceCardsMatchAvailableDice() = runTest {
        // only show roll cards for dice in dice bag
        // emphasis on checking epoch values
        // + also check epoch values in export and import code
        fail("todo")
    }
}
