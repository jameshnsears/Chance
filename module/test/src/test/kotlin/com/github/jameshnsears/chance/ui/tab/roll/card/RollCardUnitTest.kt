package com.github.jameshnsears.chance.ui.tab.roll.card

import com.github.jameshnsears.chance.utility.android.UtilityAndroid
import kotlinx.coroutines.test.runTest
import org.junit.Assert.fail
import org.junit.Test

class RollCardUnitTest : UtilityAndroid() {
    @Test
    fun deleteDiceFromBagHoldingOneDiceUsingBagModel() = runTest {
        /*
- when user rolls then ensure roll multiplier applied + show multipler icon

- when user rolls then ensure roll explosion applied immediately after value rolled + show explosion icon

- when user rolls then ensure roll score modification adjusted
         */
        fail("todo")
    }
}
